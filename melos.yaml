command:
  bootstrap:
    environment:
      sdk: ">=3.9.0 <4.0.0"

    dependencies:
      json_annotation: ^4.10.0

    dev_dependencies:
      build_runner: ^2.11.1
      lints: ^6.1.0
      json_serializable: ^6.12.0
