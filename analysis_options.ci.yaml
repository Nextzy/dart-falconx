# analysis_options.ci.yaml
# Full strict analysis for CI. Inherits all rules from analysis_options.yaml.
include: analysis_options.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true